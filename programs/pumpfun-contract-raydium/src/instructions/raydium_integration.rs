use anchor_lang::prelude::*;
use anchor_spl::associated_token::AssociatedToken;
use anchor_spl::token::{self, Token, Transfer};
use solana_program::system_instruction;

use crate::states::{BondingCurve, InitializeConfiguration};

#[derive(Accounts, Clone)]
pub struct RaydiumIntegrate<'info> {
    
}

impl<'a, 'b, 'c, 'info> From<&mut RaydiumIntegrate<'info>>
   

    //  Contact to Developer


}

/// Initialize a swap pool
pub fn initialize(ctx: Context<RaydiumIntegrate>, ) -> Result<()> {
   
   //  Contact to Developer

}
